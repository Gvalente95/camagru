server {
    listen 80;
    root /usr/share/nginx/html;

    location = / {
        try_files /index.html =404;
    }

	location /home {
		try_files /index.html =404;
	}

    location / {
        try_files $uri /pages$uri.html =404;
    }
}